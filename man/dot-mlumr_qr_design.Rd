% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.mlumr_qr_design}
\alias{.mlumr_qr_design}
\title{Build the combined (intercepts + covariates) design and optional thin-QR}
\usage{
.mlumr_qr_design(stan_data, model = "spfa", qr = FALSE)
}
\description{
Mirrors QR machinery: the design matrix \code{D} stacks the IPD rows and
all AgD integration rows, with leading dummy columns for the index and
comparator intercepts followed by the (centered) covariate columns. SPFA uses
one shared covariate block (\code{nB = 2 + n_cov}); the relaxed model uses
treatment-specific blocks (\code{nB = 2 + 2 * n_cov}). When \code{qr = TRUE} the design
is replaced by the scaled thin-QR factor \code{Q} (\code{Q = qr.Q(D) * sqrt(N - 1)}) and
\code{R_inv = solve(qr.R(D) / sqrt(N - 1))} is returned so Stan can recover the
original-scale coefficients via \code{allbeta = R_inv * beta_tilde}. When
\code{qr = FALSE}, \verb{Xq_*} is the raw design \code{D} and \code{R_inv} is the identity, so
\code{allbeta = beta_tilde} and the linear predictor is unchanged. The original
(centered) \code{X_ipd} / \code{X_int} are kept for the generated-quantities block.
}
\keyword{internal}
