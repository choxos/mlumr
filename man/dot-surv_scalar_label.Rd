% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{.surv_scalar_label}
\alias{.surv_scalar_label}
\title{Label and evaluation time for the scalar survival treatment effect}
\usage{
.surv_scalar_label(object, log_scale = FALSE)
}
\arguments{
\item{object}{An \code{mlumr_fit} (survival family).}

\item{log_scale}{\code{TRUE} for the log-scale name (as stored in \verb{delta_*}),
\code{FALSE} for the natural-scale name \code{\link[=marginal_effects]{marginal_effects()}} reports.}
}
\value{
A list with \code{label} and \code{at_time} (\code{NA} when the measure has no
evaluation time).
}
\description{
\verb{delta_*} is a marginal log hazard ratio under proportional hazards (at
\code{t -> 0} when the shapes are shared, otherwise at the first prediction
time), a log time ratio for a shared-shape SPFA AFT fit, and otherwise a
location contrast that is not a time ratio: with different shapes there is no
constant acceleration factor, and in a relaxed fit the covariate term does
not cancel. \code{\link[=marginal_effects]{marginal_effects()}} and \code{\link[=prior_sensitivity]{prior_sensitivity()}} both read this.
}
\keyword{internal}
