% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{.aux_shapes_differ}
\alias{.aux_shapes_differ}
\title{Does the baseline shape actually differ between strata?}
\usage{
.aux_shapes_differ(object)
}
\arguments{
\item{object}{An \code{mlumr_fit} (survival family).}
}
\value{
\code{TRUE} when the two studies genuinely have different baseline
shapes, \code{FALSE} otherwise.
}
\description{
\code{n_strata > 1} alone is not the question. \code{aux_by = ".study"} allocates one
auxiliary column per study, but a distribution with no shape parameter has
nothing to allocate: for the exponential (PH) and exponential-AFT the hazard
is \code{exp(eta)} and the baseline is carried entirely by the study intercept,
which is study-specific in every unanchored fit. Stratifying an exponential
therefore changes nothing, and the closed-form contrasts stay exact.
}
\details{
This mirrors the Stan gate exactly. \verb{mlumr_survival_\{spfa,relaxed\}.stan}
swaps \verb{delta_*} for the time-varying \verb{loghr_*[1]} under
\code{n_strata > 1 && nonexp && dist <= 3}, where \code{nonexp} excludes precisely the
two exponential codes; the flexible models use \code{n_strata > 1} unconditionally
because their per-stratum spline simplex IS the baseline. Reading the gate
from one helper is what stops the R layer attaching a prediction time to a
number Stan computed as the \code{t -> 0} limit.
}
\keyword{internal}
