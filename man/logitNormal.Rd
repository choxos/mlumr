% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{logitNormal}
\alias{logitNormal}
\alias{dlogitnorm}
\alias{plogitnorm}
\alias{qlogitnorm}
\title{The logit-Normal distribution}
\usage{
dlogitnorm(x, mu = 0, sigma = 1, ..., mean, sd)

plogitnorm(q, mu = 0, sigma = 1, ..., mean, sd)

qlogitnorm(p, mu = 0, sigma = 1, ..., mean, sd)
}
\arguments{
\item{x, q}{Vector of quantiles, in \verb{(0, 1)}.}

\item{mu, sigma}{Location and scale, on the logit scale.}

\item{...}{Passed to the underlying \pkg{stats} normal function.}

\item{mean, sd}{Mean and standard deviation on the \verb{(0, 1)} scale,
overriding \code{mu} and \code{sigma} when both are supplied.}

\item{p}{Vector of probabilities.}
}
\value{
A numeric vector.
}
\description{
Density, distribution, and quantile functions for the logit-normal
distribution: the distribution of \code{plogis(z)} where \code{z} is normal with mean
\code{mu} and standard deviation \code{sigma}. It is the natural marginal for a
covariate reported as a proportion on \verb{(0, 1)}, such as percent body surface
area.
}
\details{
For convenience the distribution may be given by its \code{mean} and \code{sd} on the
natural \verb{(0, 1)} scale instead of \code{mu} and \code{sigma} on the logit scale. There
is no closed form for that reparameterization, so \code{mu} and \code{sigma} are found
numerically; supply \code{mu} / \code{sigma} directly if you have them.
}
\examples{
qlogitnorm(0.5, mean = 0.34, sd = 0.19)
}
\seealso{
\code{\link[=distr]{distr()}}, \code{\link[=add_integration]{add_integration()}}, \link{GammaDist}
}
