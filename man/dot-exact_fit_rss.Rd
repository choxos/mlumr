% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.exact_fit_rss}
\alias{.exact_fit_rss}
\title{Residual sum of squares, rank, and the rounding that residual can carry}
\usage{
.exact_fit_rss(X, y, link)
}
\arguments{
\item{X}{Design matrix, intercept included.}

\item{y}{Outcome vector.}

\item{link}{\code{"identity"} or \code{"log"}.}
}
\value{
List with \code{rss}, \code{rank} and \code{zero}, or \code{NULL} when the fit did not
converge.
}
\description{
Fitted with the family and link the model itself uses, so the residual is
the one that appears in the likelihood. \code{lm.fit()}'s default pivot tolerance
of \code{1e-7} can drop a column that is nearly but not exactly collinear with
another, measuring the residual against a design smaller than the one that
will be fitted, so it is lowered to the floor. \code{glm.fit()} takes the same
tolerance as \code{min(1e-7, epsilon / 1000)}, so \code{epsilon} is its only lever.
}
\details{
\code{zero} is what the computed residual can be when the true one is zero. For
\verb{r = y - fl(X b)} the elementwise rounding is bounded by
\verb{p * eps * (|X| |b|)}, which is small when the fitted coefficients are small
and large when they are not. That is the whole discrimination: an exactly
fitting design whose coefficients run to 4e7 leaves a residual that a
well-conditioned design would only produce from real noise, and comparing
both to their own bound separates them where a single threshold cannot.
Coefficients dropped as redundant are \code{NA} and contribute nothing, so a
constant covariate needs no special handling.
}
\keyword{internal}
