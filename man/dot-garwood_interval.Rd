% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_functions.R
\name{.garwood_interval}
\alias{.garwood_interval}
\title{Exact interval for a directly observed Poisson rate}
\usage{
.garwood_interval(x, exposure, conf_level)
}
\arguments{
\item{x}{Event count, non-negative.}

\item{exposure}{Total exposure, positive.}

\item{conf_level}{Confidence level.}
}
\value{
List with \code{lower} and \code{upper}.
}
\description{
Garwood's interval, the one \code{stats::poisson.test()} reports: the lower
bound is the \code{alpha / 2} quantile of \code{Gamma(x, 1)} over the exposure and
the upper the \code{1 - alpha / 2} quantile of \code{Gamma(x + 1, 1)} over it,
with the lower bound at 0 when the count is. Coverage is at least the
nominal level for every true rate; the bounded Wald interval it replaces
ended at 0.0139 for 0 events over an exposure of 100 and covered a true
rate of 0.02 about 86\% of the time.
}
\keyword{internal}
