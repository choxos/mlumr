% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{.relaxed_contraction}
\alias{.relaxed_contraction}
\title{Marginal posterior variance change for comparator coefficients}
\usage{
.relaxed_contraction(object)
}
\arguments{
\item{object}{An \code{mlumr_fit} from \code{model = "relaxed"}.}
}
\value{
A data frame with one row per covariate (\code{covariate}, \code{prior_sd},
\code{posterior_sd}, \code{contraction}), or \code{NULL} if unavailable. The retained
\code{contraction} column is the marginal variance change defined above and is
\code{NA} where the prior has no finite variance.
}
\description{
For each \code{beta_comparator} coefficient this reports
\code{1 - posterior_variance / prior_variance}. A positive value means the
marginal posterior SD is smaller than the marginal prior SD, zero means the
two SDs are equal, and a negative value means the posterior SD is larger.
This descriptive variance comparison is not a fraction of information
learned, an identification test, or a decomposition of data and prior
influence. It ignores changes in location, shape, and posterior correlation.
}
\details{
The ratio is only interpretable against a prior with a finite variance.
\code{prior_sd} is therefore the prior STANDARD DEVIATION, not the stored scale:
for a Student-t prior they differ by \code{sqrt(df / (df - 2))}, and for \code{df <= 2}
(including the \code{df = 1} Cauchy) no finite variance exists and \code{contraction}
is \code{NA} rather than a number that would misstate what was learned.
}
\keyword{internal}
