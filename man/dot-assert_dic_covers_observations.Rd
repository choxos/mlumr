% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{.assert_dic_covers_observations}
\alias{.assert_dic_covers_observations}
\title{Refuse a cached DIC that does not cover the observations it carries}
\usage{
.assert_dic_covers_observations(dic)
}
\arguments{
\item{dic}{An \code{mlumr_dic} object.}
}
\value{
\code{TRUE} invisibly; stops otherwise.
}
\description{
\code{\link[=compare_models]{compare_models()}} accepts \code{mlumr_dic} objects beside fits and uses their
numbers as they are, so \code{\link[=.assert_log_lik_complete]{.assert_log_lik_complete()}} never sees the draws
they were computed from. \code{\link[=calculate_dic]{calculate_dic()}} used to score whatever
\code{log_lik_ipd} and \code{log_lik_agd} columns a fit held, and an object it made
then records in \code{n_obs} how many columns that was, beside the frames of the
observations the fit was built from. A saved object outlives the fix, since
loading it is not recomputing it, and the check between objects that their
\code{n_obs} agree does not see two that are partial alike.
}
\details{
The pointwise units are fixed by the data the object carries: one per index
row, and one per aggregate row, or per reconstructed comparator
pseudo-individual under survival, where the \code{pseudo} frame is present. An
\code{n_obs} that differs from that count was computed over part of the data, or
over columns that were not the fit's, and is refused: the omitted terms'
covariance with the kept ones enters the variance penalty, so the score
cannot be completed from its value. A count that agrees is consistent with
a complete score rather than proof of one, since columns misnumbered within
the right count leave no trace in a scalar. An object without its
observations, from a version before they were recorded, cannot be checked
and is compared as before, with the message that
\code{\link[=.assert_same_observations]{.assert_same_observations()}} gives for a model carrying no data; one that
carries its observations but not \code{n_obs} is compared with a message of its
own saying that the count is not recorded.
}
\keyword{internal}
