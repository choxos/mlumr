% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.mlumr_center_covariates}
\alias{.mlumr_center_covariates}
\title{Center IPD + integration covariates about their pooled mean (all families)}
\usage{
.mlumr_center_covariates(
  stan_data,
  center = TRUE,
  family = "binomial",
  agd_means = NULL
)
}
\description{
Matches \code{center = TRUE} default. The intercept then represents the
baseline at the average covariate rather than at covariate = 0, removing the
intercept<->slope collinearity that forces deep NUTS trajectories on
real-scale covariates. The likelihood is invariant because \code{X_ipd} and the
integration grid are shifted by the same \code{xbar} and the intercept absorbs the
shift. A fixed numerical intercept prior is placed on the centered intercept,
however, so centering need not leave the posterior unchanged. \code{cov_center}
is always stored (zeros when \code{center = FALSE}) so predict()/conditional_effects()
can map raw-scale covariate values onto the (possibly centered) model scale.
}
\keyword{internal}
