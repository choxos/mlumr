% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{.kept_different_rows_of_one_source}
\alias{.kept_different_rows_of_one_source}
\title{Did two frames keep different rows of one source?}
\usage{
.kept_different_rows_of_one_source(kx, ky)
}
\arguments{
\item{kx, ky}{Two \code{.source_key} columns.}
}
\value{
\code{TRUE} when the keys show one source filtered two ways.
}
\description{
Equal digests with unequal ranks are one source that two fits filtered
differently, which is what happens when the models use covariates that are
missing on different rows. That is a mismatch under every ordering
semantics: it is not a reordering, so allowing one does not allow it. Every
path through \code{\link[=.same_observations]{.same_observations()}} therefore asks this, and the paths that
permit a reordering ask only this.
}
\details{
Absent keys and different digests are left alone here. They say the rows
could not be placed, which the ordered path reports as \code{NA} and the paths
that do not depend on order have no reason to raise.
}
\keyword{internal}
