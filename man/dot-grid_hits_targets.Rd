% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.grid_hits_targets}
\alias{.grid_hits_targets}
\title{Can one affine map send every target onto a grid node?}
\usage{
.grid_hits_targets(nodes, targets)
}
\arguments{
\item{nodes}{The arm's integration nodes, one row per node.}

\item{targets}{The arm's event targets, on the scale the density matches.}
}
\value{
\code{TRUE} only where an exact map was found, \code{FALSE} where the
enumeration excluded every candidate it examined, and \code{NA} where the
case was not
decided: more than one covariate, a grid past the enumeration budget, or
a candidate that is close without being exact. An \code{NA} from the budget
carries a \code{declined} attribute of \code{"budget"}, because that is the only
one of the three that makes the same data answerable at one \code{n_int} and
unexamined at another, and the caller reports it rather than falling
silent.
}
\description{
The comparator's matching equations are solvable when some coefficient
vector sends each distinct target onto the linear predictor of some
integration node. With more distinct targets than the grid's rank that is
not automatic, and it is not impossible either: an overdetermined system
can be consistent, which is the case \code{\link[=.check_comparator_tied_events]{.check_comparator_tied_events()}}
used to skip in silence.
}
\details{
Only a single covariate is decided here, where the map is a line
\code{target = a + b * node} and two (target, node) assignments fix it, so
enumerating node pairs against the first two targets covers every
candidate. Anything wider, or a grid large enough that the enumeration
would cost more than the fit, is left undecided rather than guessed: a
false certificate here refuses a working model.

The enumeration anchors the first target at each node in turn and runs the
second anchor and every candidate node as a vectorized pass, so it costs
\code{n * n * (k - 2)} elementary operations for \code{n} nodes and \code{k} targets, not
the \code{n * n * (n + k)} of a scalar inner loop. That distinction is the
whole reach of the check: the old cost model capped it near 170 nodes, so
an \code{n_int} of 256 left unexamined the arm that 8 nodes refused.

The match must be EXACT, not merely close. A best match that leaves a
positive residual is a ridge the profile abandons as soon as the auxiliary
falls below that residual, so accepting one refuses a proper fit for a
singularity it does not have. Nodes \verb{(1, 2, 3)} against targets
\verb{(0, 1, 2 + 1e-15)} are a near miss no affine map removes, and they report
undecided rather than a match.

Consistency is therefore read off the DETERMINANT of the original data,

\code{(u[i] - u[1]) * (z[j2] - z[j1]) - (u[2] - u[1]) * (z[j] - z[j1])}

which is built from differences of the inputs and contains no division and
no slope. Rebuilding predictions from a fitted \verb{(a, b)} and comparing them
to the targets fails in both directions. It is too permissive, because the
slope can be enormous and then the terms forming a prediction dwarf the
residual: nodes \verb{(1, 1 + 2^-52, 2)} against targets \verb{(0, 1, 2)} give
\code{b = 2^52}, and any tolerance scaled by those terms accepts the target at
2 against a prediction of 1. And it is too strict, because the round trip
is inexact where the geometry is not: \code{-log(2) + log(2) * 3 == log(4)} is
FALSE while \code{log(4) - log(2) * 2 == 0} is TRUE.
}
\keyword{internal}
