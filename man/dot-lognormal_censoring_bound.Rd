% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.lognormal_censoring_bound}
\alias{.lognormal_censoring_bound}
\title{Whether a censored row bounds the log-normal scale away from zero}
\usage{
.lognormal_censoring_bound(X, y, events)
}
\arguments{
\item{X}{The full centered design, intercept first.}

\item{y}{\code{log(time)} for every row.}

\item{events}{Logical, which rows are events.}
}
\value{
\code{"bounded"}, \code{"unbounded"}, or \code{"undetermined"}.
}
\description{
The event rows are fitted exactly, so the scale collapses unless some
censored row's survival goes to zero with it. That happens when the row's
linear predictor is strictly below the log of its censoring time, and it
is a question about the exact solution, which is unique exactly when the
event design has full column rank. Without that the exact solutions form
an affine family and the censored predictors move with it.
}
\keyword{internal}
