% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{set_agd_surv}
\alias{set_agd_surv}
\title{Set up aggregate survival data (reconstructed pseudo-IPD)}
\usage{
set_agd_surv(
  data,
  treatment,
  Surv = NULL,
  time = NULL,
  status = NULL,
  entry_time = NULL,
  cov_means,
  cov_sds = NULL,
  cov_types = NULL,
  study = NULL,
  arm = NULL
)
}
\arguments{
\item{data}{Data frame of reconstructed pseudo-IPD (one row per
pseudo-individual).}

\item{treatment}{Column name for the (single) comparator treatment.}

\item{Surv}{Optional \code{\link[survival:Surv]{survival::Surv()}} object describing the outcome. Use
this for left/interval censoring or delayed entry.}

\item{time, status, entry_time}{Character column names as an alternative to
\code{Surv} (right-censoring with status \code{0}/\code{1}, plus optional delayed entry).}

\item{cov_means}{Character vector of covariate mean/proportion column names
(constant within each arm). Suffixes \verb{_mean}/\verb{_prop} are stripped to match
the IPD covariate names.}

\item{cov_sds}{Character vector of covariate SD column names (\code{NA} for binary
covariates). \code{NULL} treats all covariates as binary.}

\item{cov_types}{Character vector of \code{"continuous"}/\code{"binary"} per covariate.
If \code{NULL}, inferred from the presence of an SD column.}

\item{study}{Optional study identifier column.}

\item{arm}{Optional arm identifier column. Only a single comparator arm is
supported; if supplied, it must have one unique value.
Multi-arm reconstructed survival comparators are rejected until a
weighting estimand is implemented. Defaults to a single arm.}
}
\value{
An object of class \code{mlumr_agd_surv} (also inheriting \code{mlumr_agd}).
Its \verb{$pseudo_ipd} carries a \code{.source_key} column as \code{\link[=set_ipd]{set_ipd()}} describes:
a digest of the whole of \code{data} with the row's rank within a canonical
ordering of it, holding nothing of the content, so that
\code{\link[=compare_models]{compare_models()}} can recognize one source reordered between two fits.
The internal names, \code{.source_key} among them, cannot be used as column
names in \code{data}.
}
\description{
Prepare comparator aggregate survival data for an unanchored indirect
comparison. The comparator arm is supplied as \strong{reconstructed pseudo-IPD}
(event/censoring times digitized from a published Kaplan-Meier curve, e.g.
via the Guyot algorithm) together with summary covariate moments
(means/SDs). The Stan model integrates the comparator likelihood over the
covariate distribution implied by those moments.
}
\details{
Under delayed entry the comparator likelihood conditions each integration
point on survival to its entry time and then averages, so \code{cov_means},
\code{cov_sds} and the distributions \code{\link[=add_integration]{add_integration()}} builds must describe
the population observed at entry (those in the risk set), not a baseline
population before selection. With varying entry times that population can
differ by entry time while the model has one distribution per arm; pooled
summaries are right only under a common entry time or when the covariate
distribution among those observed at entry is the same at every entry
time. Neither condition is checkable from the summaries supplied. Delayed
entry in the individual arm is unaffected.
}
\section{Reconstruction uncertainty is not propagated}{

The pseudo-individual records enter the likelihood as observed data, so
credible intervals are conditional on this one reconstruction and narrower
than the evidence supports. Treat the reconstruction as an analysis choice:
digitize the curve more than once or perturb the points within their
reading error, refit, and report the spread across refits beside the
within-fit interval.
}

\examples{
\dontrun{
agd <- set_agd_surv(
  data = comparator_km,
  treatment = "trt",
  time = "time", status = "status",
  cov_means = c("age_mean", "male_prop"),
  cov_sds = c("age_sd", NA),
  cov_types = c("continuous", "binary")
)
}
}
\seealso{
\code{\link[=set_agd]{set_agd()}} for non-survival aggregate data;
\code{multinma::set_agd_surv()} is the ML-NMR equivalent.
}
