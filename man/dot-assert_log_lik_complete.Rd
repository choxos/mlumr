% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{.assert_log_lik_complete}
\alias{.assert_log_lik_complete}
\title{Refuse a pointwise log-likelihood that does not cover the fitted data}
\usage{
.assert_log_lik_complete(object)
}
\arguments{
\item{object}{An \code{mlumr_fit} object.}
}
\value{
\code{TRUE} invisibly; stops otherwise.
}
\description{
LOO, WAIC, and DIC score whatever \code{log_lik_ipd} and \code{log_lik_agd} columns
the saved draws hold, and extracting them used to stop only when both were
absent. rstan's \code{pars} with \code{include = FALSE}, passed through the \code{...} of
\code{\link[=mlumr]{mlumr()}}, saves a fit without either block, and a fit object edited
afterwards can lose single columns. The criteria were then computed over
part of the data the model was fitted to, with only the column count to
show for it, and two fits missing the same block were compared without
complaint although that part can rank them differently than all of it.
}
\details{
The columns that belong are fixed by the Stan models: \code{log_lik_ipd} holds
one per index observation, \code{1:n_ipd}, and \code{log_lik_agd} one per aggregate
row, \code{1:n_agd_rows}, except under survival, where the comparator enters as
reconstructed pseudo-individuals and there is one per pseudo-individual,
\code{1:n_agd}. Outside survival \code{n_agd} is each row's sample size, not a column
count. Where tied rows were collapsed, the expanded count \code{sum(agd_count)}
is what has to be there, and \code{\link[=.assert_agd_loglik_per_observation]{.assert_agd_loglik_per_observation()}} has
already refused the collapsed shape. The indexes are compared as a set, so
a missing column, a repeated one, and a misnumbered one that keeps the
count are all refused, and so is a column named as a pointwise value whose
index cannot be read.
}
\keyword{internal}
