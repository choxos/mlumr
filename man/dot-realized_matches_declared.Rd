% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identification.R
\name{.realized_matches_declared}
\alias{.realized_matches_declared}
\title{Does the realized integration design reproduce the declared one?}
\usage{
.realized_matches_declared(
  declared,
  realized,
  ref_sd = NULL,
  max_location_gap = 0.25
)
}
\arguments{
\item{declared}{Matrix of declared mean profiles, rows by covariates.}

\item{realized}{Matrix of realized integration means, or \code{NULL}.}

\item{ref_sd}{Reference SD per covariate; \code{NULL} falls back to each
declared column's range.}

\item{max_location_gap}{Largest per-row distance, in reference SDs, that
still counts as a match.}
}
\description{
Compares each row's realized integration means with its declared
\verb{<covariate>_mean} values, in reference SDs. A finite grid misses its
declared mean by a few hundredths of an SD, while a \code{distr()} that ignores
its row misses by the whole distance to whatever it was given, so a quarter
of an SD separates the two. \code{TRUE} when there is nothing to compare, or
when the two cannot be compared (with a warning).
}
\keyword{internal}
