% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{.require_identity}
\alias{.require_identity}
\title{Refuse a missing grouping identifier}
\usage{
.require_identity(x, nm, as_char = TRUE)
}
\arguments{
\item{x}{The column.}

\item{nm}{Its name, for the message.}

\item{as_char}{Return \code{as.character(x)} rather than \code{x}.}
}
\description{
\code{unique()} keeps \code{NA} and \code{which(NA == NA)} selects nothing, so an arm
column of \code{NA} passed the single-arm check and then matched no rows: the
summary came back with \code{.study}, \code{.trt}, and every covariate mean \code{NA},
from data that carried a treatment and a mean. The object was structurally
valid, so nothing downstream objected to integrating over \code{NA}.
}
\keyword{internal}
