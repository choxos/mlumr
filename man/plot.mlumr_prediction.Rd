% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.mlumr_prediction}
\alias{plot.mlumr_prediction}
\title{Plot absolute predictions from a fitted ML-UMR model}
\usage{
\method{plot}{mlumr_prediction}(x, ref_line = NULL, ...)
}
\arguments{
\item{x}{A \code{predict()} result (an \code{mlumr_prediction}).}

\item{ref_line}{Optional numeric null-reference line(s). Drawn as horizontal
line(s) for curve types and vertical line(s) for scalar types, mirroring the
\code{plot(predict(fit), ref_line = c(0, 1))} idiom (e.g. probability bounds for
\code{type = "response"}). The log-hazard-ratio curve defaults to \code{ref_line = 0}.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object (compose further layers, e.g. a KM overlay, with \code{+}).
}
\description{
Dispatches on the prediction \code{type}: time-indexed types
(\code{"survival"}, \code{"hazard"}, \code{"cumhaz"}, \code{"loghr"}) are drawn as curves with
95\% credible bands; scalar types (\code{"rmst"}, \code{"median"}, \code{"response"}) as
point-intervals. The mlumr analogue of \code{plot(predict(multinma_fit))}.
}
\examples{
\dontrun{
plot(predict(fit, type = "survival")) + geom_km(dat)
plot(predict(fit, type = "response"), ref_line = c(0, 1))
plot(predict(fit, type = "loghr"))
}
}
\seealso{
\code{\link[=predict.mlumr_fit]{predict.mlumr_fit()}}, \code{\link[=geom_km]{geom_km()}}
}
