% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.exact_sum_sign}
\alias{.exact_sum_sign}
\title{The sign of an exact sum of doubles}
\usage{
.exact_sum_sign(terms)
}
\arguments{
\item{terms}{As for \code{\link[=.exact_sum_is_zero]{.exact_sum_is_zero()}}.}
}
\value{
\code{-1}, \code{0} or \code{1}, or \code{NA} where any term is not finite.
}
\description{
The same expansion as \code{\link[=.exact_sum_is_zero]{.exact_sum_is_zero()}}, answering which side of zero
the sum falls on rather than only whether it is zero. The components come
out non-overlapping and in increasing magnitude, so the LAST nonzero one
carries the sign: everything below it is too small to reach its bits, let
alone cross zero.
}
\details{
This is what a comparison between two exact quantities needs. Deciding
whether a candidate slope \code{p / q} satisfies \code{c <= beta * d} by computing
the division and comparing is a floating-point solve, and a solve cannot
certify anything; cross-multiplying turns it into the sign of
\code{p * d - c * q}, which this answers exactly.
}
\keyword{internal}
