% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.design_replicates}
\alias{.design_replicates}
\title{Replicate design rows, and whether their outcomes agree}
\usage{
.design_replicates(X, y)
}
\arguments{
\item{X}{Raw design matrix.}

\item{y}{Outcome vector.}
}
\value{
List with \code{n_distinct}, the number of distinct design rows, and
\code{consistent}, \code{FALSE} if some replicate group carries more than one
outcome value.
}
\description{
Rows with identical covariates get identical fitted values under any model,
so two such rows with different outcomes leave a residual that no fit can
remove. That is a structural fact, not a numerical one: it proves the
residual sum of squares positive without measuring it. Rows are compared on
their exact binary representation, since \code{paste()} on doubles keeps fifteen
digits and could merge two rows that differ.
}
\keyword{internal}
