% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{.sorted_keys}
\alias{.sorted_keys}
\title{Sort key strings in byte order}
\usage{
.sorted_keys(keys)
}
\arguments{
\item{keys}{A character vector of keys or digests.}
}
\value{
The same values in byte order.
}
\description{
The keys are compared by sorting both sides and testing the results for
equality, so the order has to be a total order on the strings themselves.
The default collation follows \code{LC_COLLATE}, which can rank two distinct
strings as equal, and two equal multisets then sort into two different
vectors and read as a mismatch. Byte order is total and the same everywhere,
which is what \code{\link[=.source_row_keys]{.source_row_keys()}} builds the keys in.
}
\keyword{internal}
