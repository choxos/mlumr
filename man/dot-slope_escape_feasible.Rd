% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.slope_escape_feasible}
\alias{.slope_escape_feasible}
\title{Is a bounded slope still able to escape one more half-line?}
\usage{
.slope_escape_feasible(pairs, cc, dd, cc_err = 0, dd_err = 0)
}
\arguments{
\item{pairs}{The region's conditions, as \code{\link[=.slope_region_pairs]{.slope_region_pairs()}} returns.
\code{NULL} means no condition, which leaves the extra one feasible on its
own.}

\item{cc, dd}{The extra condition \code{cc <= beta * dd}, and}

\item{cc_err, dd_err}{the errors their own subtractions left.}
}
\value{
\code{1} where some slope satisfies every condition STRICTLY, \code{0} where
the only solutions sit on a boundary (the feasible set is a single
point, whose coefficient volume costs a power not counted by the
caller), \code{-1} where there is no solution, and \code{NA} where the arithmetic
could not settle it.
}
\description{
The index region's conditions and one further condition of the same shape,
\verb{c <= beta d}, read together as a one-dimensional feasibility question. A
condition with \code{d > 0} is a lower end for \code{beta} and one with \code{d < 0} an
upper end, so the system has a solution exactly when every lower end is at
or below every upper end. Cross-multiplied, that is
\verb{c_k d_l - c_l d_k >= 0} for each such pair, which is the sign
\code{\link[=.cross_sign]{.cross_sign()}} decides; the division is never taken, because a
floating-point solve certifies nothing.
}
\keyword{internal}
