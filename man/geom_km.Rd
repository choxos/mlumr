% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{geom_km}
\alias{geom_km}
\title{Observed Kaplan-Meier layer for survival overlays}
\usage{
geom_km(data, treatments = NULL, marks = TRUE, linewidth = 0.4, ...)
}
\arguments{
\item{data}{An \code{mlumr_data} survival object from \code{\link[=combine_data]{combine_data()}}.}

\item{treatments}{Optional character vector of treatment labels to draw. By
default both observed arms are drawn; pass \code{data$comparator_treatment} to
overlay only the comparator KM on a comparator-population prediction (so the
observed and predicted curves refer to the same population).}

\item{marks}{Logical; draw censoring marks (default \code{TRUE}).}

\item{linewidth}{Step line width (default \code{0.4}).}

\item{...}{Passed to \code{\link[ggplot2:geom_step]{ggplot2::geom_step()}}.}
}
\value{
A list of ggplot2 layers (a step layer, plus a censoring-mark layer
when \code{marks = TRUE}) to add to a plot with \code{+}. The layers carry the
population each observed arm was measured in, so on a plot faceted by
population each curve appears only in its own panel. A plot standardized to
a \code{newdata} target therefore shows no observed curve, which is correct:
no arm was observed in that population.
}
\description{
Returns ggplot2 layers drawing the observed Kaplan-Meier step curves (the
index IPD and the reconstructed comparator pseudo-IPD) of a survival
\code{mlumr_data}, colored by treatment so they line up with a survival
prediction plot. The mlumr analogue of multinma's \code{geom_km()}, so a
predicted-versus-observed figure is just
\code{plot(predict(fit, type = "survival")) + geom_km(data)}.
}
\examples{
\dontrun{
plot(predict(fit, type = "survival")) + geom_km(dat)
plot(predict(fit, type = "survival")) + geom_km(dat, dat$comparator_treatment)
}
}
\seealso{
\code{\link[=plot.mlumr_prediction]{plot.mlumr_prediction()}}
}
