% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{mlumr_forest}
\alias{mlumr_forest}
\title{Forest plot of a small set of estimates}
\usage{
mlumr_forest(
  data,
  ref_line = 0,
  log_x = FALSE,
  x = NULL,
  title = NULL,
  subtitle = NULL,
  color = "#3B6B9A",
  clip = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame with one row per estimate. Columns are matched
flexibly (first match wins): the row label from \code{label} / \code{method} /
\code{Method} / \code{Comparison} (else the first character/factor column); the point
estimate from \code{est} / \code{estimate} / \code{mean}; the interval bounds from
\code{lo}/\code{hi}, \code{q2.5}/\code{q97.5}, \code{ci_lower}/\code{ci_upper}, \code{conf.low}/\code{conf.high}, or
\code{lower}/\code{upper}.}

\item{ref_line}{Null-effect reference line (default \code{0}; pass \code{1} for ratio
measures).}

\item{log_x}{Draw the x axis on a log10 scale (for ratio measures).}

\item{x, title, subtitle}{Axis label and titles (passed to \code{\link[ggplot2:labs]{ggplot2::labs()}}).}

\item{color}{Point and interval color.}

\item{clip}{Logical; if \code{TRUE} (default), when one or two intervals are far
wider than the rest the x axis is clipped to the bulk of the estimates and
the over-wide interval's clipped end(s) are drawn with an arrow, so a single
very uncertain estimate does not compress all the others into a sliver.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.
}
\description{
A one-call forest plot for comparing a handful of estimates supplied as a data
frame, e.g. several methods (naive / STC / ML-UMR) or several covariate
profiles. Rows are drawn top-to-bottom in the order given. Returns a \code{ggplot}
object, so further ggplot2 layers compose with \code{+}. This keeps the
method-comparison forests in the vignettes to a single line instead of a
hand-built \code{ggplot()} stack.
}
\examples{
\dontrun{
forest_df <- data.frame(
  label = c("Naive", "STC", "ML-UMR SPFA"),
  est = c(res_naive$estimate, res_stc$estimate, me$mean),
  lo = c(res_naive$ci_lower, res_stc$ci_lower, me$q2.5),
  hi = c(res_naive$ci_upper, res_stc$ci_upper, me$q97.5)
)
mlumr_forest(forest_df, ref_line = 0, x = "Log odds ratio")
}
}
\seealso{
\code{\link[=marginal_effects]{marginal_effects()}}, \code{\link[=naive]{naive()}}, \code{\link[=stc]{stc()}}
}
