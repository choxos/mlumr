% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exact_geometry.R
\name{.exact_rank}
\alias{.exact_rank}
\title{Exact rank of a matrix of doubles}
\usage{
.exact_rank(X)
}
\arguments{
\item{X}{A matrix of finite doubles.}
}
\value{
List with \code{rank} and \code{pivots}, a set of \code{rank} columns that is
exactly linearly independent.
}
\description{
Every double is a rational number, \code{m * 2^e} with an odd integer \code{m} below
\code{2^53}, so each column can be scaled by a power of two into a column of
integers, and the rank of that integer matrix is the exact rank of the
input. The rank is found modulo primes: the rank modulo \code{p} never exceeds
the exact rank, and it falls short only when \code{p} divides every nonzero
minor of the exact rank's size. Hadamard's inequality bounds those minors,
so once the product of the primes used exceeds the bound some prime sees
the full rank, and the largest rank found is the exact one. Residues below
\code{2^26} multiply to below \code{2^52}, so the whole computation is exact.
}
\details{
This is what lets a guard reason about the design the model fits rather
than about the one a floating-point factorization resolves: a column that
differs from a combination of the others by \code{1e-20} is independent of
them, and a rank computed at machine precision cannot see that. The
pivot columns returned are exactly independent, which a numerical pivot
set is not guaranteed to be.
}
\keyword{internal}
