% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional_effects.R
\name{.surv_eval_curve}
\alias{.surv_eval_curve}
\title{Evaluate the conditional survival curve S(t | profile) per posterior draw}
\usage{
.surv_eval_curve(object, eta, treatment = c("index", "comparator"))
}
\arguments{
\item{object}{An \code{mlumr_fit} (survival).}

\item{eta}{Numeric vector (length n_draws) of the per-draw linear predictor
at one covariate profile for one treatment.}

\item{treatment}{Which arm's baseline hazard to evaluate against. With
\code{aux_by = ".study"} (the default) each study has its own baseline, so the
curve depends on which arm is being predicted.}
}
\value{
A matrix of survival probabilities, draws (rows) by fitted
prediction times (columns).
}
\description{
Evaluate the conditional survival curve S(t | profile) per posterior draw
}
\keyword{internal}
