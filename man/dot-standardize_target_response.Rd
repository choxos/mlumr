% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{.standardize_target_response}
\alias{.standardize_target_response}
\title{Target-population standardized response means per treatment (g-computation)}
\usage{
.standardize_target_response(object, newdata)
}
\arguments{
\item{object}{An \code{mlumr_fit} (binomial / normal / poisson).}

\item{newdata}{Data frame of target-population covariate profiles.}
}
\value{
A list with \code{index} and \code{comparator}, each a length-\code{n_draws} vector
of target-standardized marginal response means.
}
\description{
Bayesian g-computation / model-based standardization of the per-treatment
marginal mean outcome over an arbitrary target population, reusing the
validated conditional-effects machinery (\verb{.conditional_*}). For each posterior
draw and treatment k, returns
\verb{mu_k = (1/M) sum_m g^\{-1\}(alpha_k + (x_m - xbar)' beta_k)}
averaged over the \code{M} rows of \code{newdata} (the target covariate distribution).
Non-survival families only.
}
\keyword{internal}
