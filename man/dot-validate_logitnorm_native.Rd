% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{.validate_logitnorm_native}
\alias{.validate_logitnorm_native}
\title{Validate native logit-normal \code{mu} / \code{sigma}}
\usage{
.validate_logitnorm_native(mu, sigma)
}
\arguments{
\item{mu, sigma}{Logit-scale location and scale.}
}
\value{
A list with validated \code{mu} and \code{sigma}.
}
\description{
The moment parameterization has always been checked; the native one was
passed through untouched, so one invalid \code{sigma} produced three different
answers depending on which function saw it. \code{sigma = 0} gave \code{Inf} from
\code{dlogitnorm()}, \code{1} from \code{plogitnorm()} and \code{0.5} from \code{qlogitnorm()}, all
finite and none flagged, while \code{sigma = -1} gave \code{NA} from the density and
\code{NaN} with a base warning from the other two. A degenerate spike is not a
distribution \code{add_integration()} can draw from, so this is an error in both
parameterizations rather than a value that silently propagates.
}
\keyword{internal}
