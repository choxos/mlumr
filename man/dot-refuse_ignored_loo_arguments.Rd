% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{.refuse_ignored_loo_arguments}
\alias{.refuse_ignored_loo_arguments}
\title{Refuse arguments that loo's matrix methods would drop}
\usage{
.refuse_ignored_loo_arguments(args, accepted, fun)
}
\arguments{
\item{args}{The caller's \code{...}, as a list.}

\item{accepted}{The further arguments the matrix method does read, from
\code{\link[=.loo_matrix_reads]{.loo_matrix_reads()}}.}

\item{fun}{The caller's name, for the message.}
}
\value{
\code{TRUE} invisibly; stops otherwise.
}
\description{
\code{\link[=calculate_loo]{calculate_loo()}} and \code{\link[=calculate_waic]{calculate_waic()}} hand \code{loo} a log-likelihood
matrix, and the matrix methods read only their own formals: any other
argument is accepted through \code{...} and dropped, so the estimate comes back
as though it had never been asked for. \code{moment_match = TRUE} was the
documented case. Moment matching re-evaluates the posterior and each
observation's likelihood at transformed draws, which needs the fitted
model, and a matrix does not carry it.
}
\keyword{internal}
