% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.two_prod_err}
\alias{.two_prod_err}
\title{The rounding error of a floating-point product, exactly}
\usage{
.two_prod_err(a, b, p)
}
\arguments{
\item{a, b}{The operands.}

\item{p}{Their computed product.}
}
\value{
The exact rounding error.
}
\description{
Dekker's TwoProduct by splitting, since R exposes no fused multiply-add.
Each operand is cut into two halves of at most 26 significant bits, whose
pairwise products are exact, so for \code{p = a * b} the returned \code{e} satisfies
\code{a * b = p + e} exactly. \code{e == 0} says the multiplication was exact.
}
\details{
The split multiplies by \code{2^27 + 1}, so an operand within a factor of
\code{2^27} of the overflow threshold returns a non-finite error. That reads as
"not exact", which is the safe direction here.
}
\keyword{internal}
