% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.cross_sign}
\alias{.cross_sign}
\title{The exact sign of a two-by-two cross difference, or NA}
\usage{
.cross_sign(c1, d1, c2, d2, e1 = 0, f1 = 0, e2 = 0, f2 = 0)
}
\arguments{
\item{c1, d1, c2, d2}{The four operands.}

\item{e1, f1, e2, f2}{Their respective errors, \code{0} where a value is exact.}
}
\value{
\code{-1}, \code{0}, \code{1}, or \code{NA} where the arithmetic could not settle it.
}
\description{
\verb{c1 d2 - c2 d1} where each operand carries the error its own subtraction
left, so the quantity whose sign is wanted is
\code{(c1 + e_c1)(d2 + e_d2) - (c2 + e_c2)(d1 + e_d1)}. Expanding that exactly
is sixteen terms; the cheap four-term sign is taken instead and accepted
only where the four-term value provably dominates what the errors can
contribute. Genuine cancellation at the last bits is left undecided.
}
\keyword{internal}
