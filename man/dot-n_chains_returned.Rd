% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend_cmdstanr.R
\name{.n_chains_returned}
\alias{.n_chains_returned}
\title{Number of chains actually present in the returned draws}
\usage{
.n_chains_returned(chain_ids, chains)
}
\arguments{
\item{chain_ids}{Per-draw chain labels, or \code{NULL} when unavailable.}

\item{chains}{Number of chains requested.}
}
\value{
Integer count of chains present.
}
\description{
A chain can terminate abnormally (for example a numerically fragile
likelihood whose gradient fails to evaluate). Both backends then return a fit
assembled from the surviving chains only, so the posterior silently
represents fewer chains than were requested. Counting the distinct chain ids
in the draws is the one check that catches this on either engine.
}
\keyword{internal}
