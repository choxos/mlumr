% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.slope_region_pairs}
\alias{.slope_region_pairs}
\title{The slope conditions an index's observation regions leave}
\usage{
.slope_region_pairs(region)
}
\arguments{
\item{region}{The index's design and region ends, as
\code{\link[=.check_survival_scale_collapse]{.check_survival_scale_collapse()}} reports in its \code{index_region}
attribute.}
}
\value{
A list of \code{cc}, \code{dd} and the errors \code{c_err}, \code{d_err} their own
subtractions left, each condition reading \code{cc <= beta * dd}. \code{NULL}
where the region leaves no condition on the slope or was not usable,
which a caller reads as no restriction at all.
}
\description{
Every index row is observed to lie in a region: a censored row in the
interval its censoring puts it in, an event row at the single point its
own time puts it at. As the auxiliary goes to its boundary a row's
contribution tends to one where its linear predictor is inside that region
and vanishes exponentially where it is outside, and an event row's density
grows where the predictor reproduces its time and vanishes anywhere else.
So the coefficients keeping the index alive are the ones satisfying every
row at once:
}
\details{
\verb{lower_i <= mu + beta' x_i <= upper_i}.

Under \code{model = "spfa"} with \code{aux_by = "none"} the comparator shares \code{beta}
AND the auxiliary, so any divergence of its own needs a slope this region
still allows. Eliminating \code{mu} between a row with a finite lower end and
one with a finite upper end (Fourier-Motzkin) leaves

\code{lower_i - upper_j <= beta (x_i - x_j)},

one linear condition on the slope per such pair. Pairs at the same
covariate value say nothing about the slope; they are a conflict or not,
and \code{\link[=.censoring_bounds_aux]{.censoring_bounds_aux()}} has already answered that.

The conditions are returned rather than solved, because two different
questions are asked of them: whether one particular candidate slope is
allowed (\code{\link[=.index_slope_admits]{.index_slope_admits()}}), and whether any allowed slope also
satisfies one further condition (\code{\link[=.slope_escape_feasible]{.slope_escape_feasible()}}). Both read
the same representation.
}
\keyword{internal}
