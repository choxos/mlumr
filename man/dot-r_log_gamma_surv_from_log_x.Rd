% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional_effects.R
\name{.r_log_gamma_surv_from_log_x}
\alias{.r_log_gamma_surv_from_log_x}
\title{Log upper regularized gamma from the log of its argument, in R}
\usage{
.r_log_gamma_surv_from_log_x(k, log_x)
}
\arguments{
\item{k}{Shape, recycled to the length of \code{log_x}.}

\item{log_x}{Log of the incomplete-gamma argument.}
}
\value{
Log survival, the same length as \code{log_x}.
}
\description{
Mirrors Stan's \code{log_gamma_surv_from_log_x()}. \code{exp(log_x)} underflows to
zero below about -745 and \code{pgamma(0, k)} then reports survival 1, which is
badly wrong for a small shape: the survival depends on \code{w^k}, which is
\code{exp(k * log_x)} and stays of order one however far the log has gone. At
\code{k = 1e-6} and \code{log_x = -1013.8} the true value is 0.0010127 and the
underflowed one is exactly 1.
}
\details{
Below the threshold the leading term of the series for the lower regularized
gamma, \code{w^k / gamma(k + 1)}, is exact to double precision, because the next
term is smaller by a factor of \code{w}. A \code{log_x} of negative infinity, which is
time zero, still gives survival 1.

Every R site needing this quantity calls here, so the survival and the
hazard cannot disagree with each other or with Stan. They did: the
correction was at first applied only to the generalized-gamma survival,
which left the gamma survival reporting 1, the gamma hazard too small by a
factor of 1000, and the generalized-gamma hazard by 987. Stan routes all
four through one function, so R does too.
}
\keyword{internal}
