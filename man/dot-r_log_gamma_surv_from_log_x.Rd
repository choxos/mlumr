% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional_effects.R
\name{.r_log_gamma_surv_from_log_x}
\alias{.r_log_gamma_surv_from_log_x}
\title{Log upper regularized gamma from the log of its argument, in R}
\usage{
.r_log_gamma_surv_from_log_x(k, log_x)
}
\arguments{
\item{k}{Shape, recycled to the length of \code{log_x}.}

\item{log_x}{Log of the incomplete-gamma argument.}
}
\value{
Log survival, the same length as \code{log_x}.
}
\description{
Mirrors Stan's \code{log_gamma_surv_from_log_x()}. Below a \code{log_x} of -700 the
leading series term \code{w^k / gamma(k + 1)} is exact, where \code{exp(log_x)}
would underflow and \code{pgamma(0, k)} report survival 1. Every R site needing
this quantity calls here, as every Stan site does.
}
\keyword{internal}
