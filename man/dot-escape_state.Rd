% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.escape_state}
\alias{.escape_state}
\title{Can a bounded slope carry a node past a one-sided censoring threshold?}
\usage{
.escape_state(pairs, nodes, target, threshold, side)
}
\arguments{
\item{pairs}{The index region's conditions, as \code{\link[=.slope_region_pairs]{.slope_region_pairs()}}
returns.}

\item{nodes}{The arm's integration nodes, one row per node.}

\item{target}{The arm's single distinct event target.}

\item{threshold}{The binding end of the threatening rows' satisfied sets:
the largest lower end where they are all satisfied above, the smallest
upper end where they are all satisfied below.}

\item{side}{\code{"above"} or \code{"below"}.}
}
\value{
\code{"inside"} where some admitted slope escapes STRICTLY, \code{"outside"}
where no admitted slope escapes at all, and \code{"boundary"} where the only
escapes sit on a boundary or the arithmetic could not settle it.
}
\description{
The arm's event rows are all at ONE target, so they are matched at a
single node \code{z_m} and leave the slope free by themselves; every node then
sits at \code{t + beta (z_j - z_m)}. A threatening censored row is escaped
where some node's predictor reaches its threshold, which is one more
condition of the same shape the index's own region has,
\code{c <= beta (z_j - z_m)}, with \code{c} positive because a row satisfied AT the
target is not threatening in the first place.
}
\details{
Only the widest separation matters, in each direction. A larger \verb{|d|} is a
weaker condition when \code{c > 0}, so \code{max(z) - min(z)} and its negative
dominate every other node pair, and the quadratic scan over pairs is not
needed.
}
\keyword{internal}
