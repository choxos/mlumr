% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_sensitivity.R
\name{.prior_sensitivity_merge_dots}
\alias{.prior_sensitivity_merge_dots}
\title{A caller's \code{...} merged into the arguments that replay a fit}
\usage{
.prior_sensitivity_merge_dots(call_args, dots)
}
\arguments{
\item{call_args}{The replay arguments built from the original fit.}

\item{dots}{The caller's \code{...}, as a list.}
}
\value{
\code{call_args}, with the caller's settings merged in.
}
\description{
The recorded \code{control} describes the original fit, so a caller's settings
refine it rather than replace it, and the engine that will actually run has
to be resolved the way \code{mlumr()} resolves it rather than read off the call.
Split out from the refit loop so the merge can be checked without sampling:
written out at the call site it could only be tested by a copy, and a copy
passes whatever the loop itself then does.
}
\keyword{internal}
