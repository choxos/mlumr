% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlumr.R
\name{.zero_boundary}
\alias{.zero_boundary}
\title{Whether zero rows can be sent to the boundary while positive rows stay fit}
\usage{
.zero_boundary(X_pos, X_zero)
}
\arguments{
\item{X_pos}{Centered design rows of the positive outcomes.}

\item{X_zero}{Centered design rows of the zero outcomes.}
}
\value{
\code{"reachable"}, \code{"unreachable"} or \code{"unknown"}.
}
\description{
Under a log link a zero outcome is matched only in the limit where its
linear predictor goes to \code{-Inf}. The residual goes to zero along a ray in
coefficient space exactly when some direction \code{d} leaves every positive
row's predictor unchanged, \verb{X_pos d = 0}, and lowers every zero row's,
\verb{X_zero d < 0}. A rank deficit in \code{X_pos} is necessary for that and not
sufficient: with positive rows at \code{x = 0} and zeros at \code{x = -1} and \code{x = 1}
the one free direction moves the two zero rows in opposite directions, and
their means stay bounded away from zero.
}
\details{
The question is a linear feasibility one. With \code{k} free directions it is
decided exactly for \code{k <= 2}: a zero row whose profile lies in the row space
of the positive rows is pinned and settles it; for one direction the zero
rows' loadings must share a sign; for two, their loadings must lie in an
open half-plane through the origin, which is a gap of more than pi between
consecutive angles. Beyond two the answer is left unknown, and the caller
refuses conservatively.
}
\keyword{internal}
