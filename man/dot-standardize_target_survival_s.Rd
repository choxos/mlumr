% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{.standardize_target_survival_s}
\alias{.standardize_target_survival_s}
\title{Target-population standardized survival curve S-bar(t) per treatment}
\usage{
.standardize_target_survival_s(
  object,
  newdata,
  times,
  ibasis,
  ibasis_cmp = NULL,
  log_scale = FALSE
)
}
\value{
A list with \code{index} and \code{comparator}, each an \verb{[n_draws, length(times)]}
matrix of target-standardized survival probabilities, and \code{share}, a
list of two per-draw vectors named the same way.
}
\description{
g-computation of the population-average survival function over an arbitrary
target population (Chandler & Ishak Eq 14): for each treatment,
\verb{S_bar_k(t) = (1/M) sum_m S_k(t | x_m)} over the \code{M} rows of \code{newdata}.
}
\details{
The \code{share} element is the resolution share of \code{\link[=.decay_share]{.decay_share()}}, measured
on each profile's curve before averaging and summed over profiles: the
average can look resolved when none of its parts is.
}
\keyword{internal}
