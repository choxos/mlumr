# Warn that survival LOO/WAIC pointwise units are reconstructed pseudo-IPD

Once per session; suppress with
`options(mlumr.quiet_survival_loo = TRUE)`.

## Usage

``` r
.warn_survival_loo_unit(object)
```
