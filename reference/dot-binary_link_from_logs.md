# Binary link of a marginal probability represented on both log tails

Binary link of a marginal probability represented on both log tails

## Usage

``` r
.binary_link_from_logs(
  log_event,
  log_nonevent,
  link = c("logit", "probit", "cloglog")
)
```
