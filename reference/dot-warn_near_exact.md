# The near-exact warning, shared by the plain and the mixed-zero paths

The near-exact warning, shared by the plain and the mixed-zero paths

## Usage

``` r
.warn_near_exact(ratio)
```

## Arguments

- ratio:

  Residual sum of squares over the total.

## Value

`NULL`, invisibly; called for the warning.
