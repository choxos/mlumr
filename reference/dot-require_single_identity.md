# Refuse an arm that spans more than one study

Refuse an arm that spans more than one study

## Usage

``` r
.require_single_identity(values, arm_vec, label)
```

## Arguments

- values:

  The identifier to check within each arm.

- arm_vec:

  Arm labels.

- label:

  Name of the identifier, for the message.
