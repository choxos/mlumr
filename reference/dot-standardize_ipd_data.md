# Standardize IPD to mlumr's internal column contract

Standardize IPD to mlumr's internal column contract

## Usage

``` r
.standardize_ipd_data(
  data,
  treatment,
  outcome,
  covariates,
  family,
  exposure = NULL,
  study = NULL
)
```
