# Note the index-population extrapolation in relaxed fits

Emitted from
[`marginal_effects()`](https://choxos.github.io/mlumr/reference/marginal_effects.md)
(and the survival dispatch). Once per call; suppress with
`options(mlumr.quiet_relaxed_index = TRUE)`.

## Usage

``` r
.relaxed_index_note(object, population)
```
