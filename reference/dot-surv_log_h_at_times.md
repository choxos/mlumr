# Conditional log hazard at arbitrary times for one predictor draw vector

Conditional log hazard at arbitrary times for one predictor draw vector

## Usage

``` r
.surv_log_h_at_times(
  object,
  eta,
  times,
  mbasis = NULL,
  treatment = c("index", "comparator")
)
```
