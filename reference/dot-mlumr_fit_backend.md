# Dispatch mlumr() sampling to the selected backend

Dispatch mlumr() sampling to the selected backend

## Usage

``` r
.mlumr_fit_backend(
  engine,
  model_name,
  stan_data,
  chains,
  iter,
  warmup,
  seed,
  adapt_delta,
  max_treedepth,
  refresh,
  verbose = TRUE,
  ...
)
```
