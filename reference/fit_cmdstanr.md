# Fit a Stan model using cmdstanr

Fit a Stan model using cmdstanr

## Usage

``` r
fit_cmdstanr(
  model_name,
  stan_data,
  chains,
  iter,
  warmup,
  seed,
  adapt_delta,
  max_treedepth,
  refresh,
  verbose = TRUE,
  ...
)
```
