# Mean covariate profile actually realized by each row's integration points

Mean covariate profile actually realized by each row's integration
points

## Usage

``` r
.agd_realized_profiles(data, covs)
```
