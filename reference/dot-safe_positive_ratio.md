# Ratio with the same positive-denominator guard used by Stan generated quantities

Ratio with the same positive-denominator guard used by Stan generated
quantities

## Usage

``` r
.safe_positive_ratio(num, denom)
```
