# Validate mlumr() sampler controls before backend dispatch

Validate mlumr() sampler controls before backend dispatch

## Usage

``` r
.validate_mlumr_sampling_args(
  chains,
  iter,
  warmup,
  seed,
  adapt_delta,
  max_treedepth,
  refresh
)
```
