# Build the per-arm covariate-summary table for survival AgD

Build the per-arm covariate-summary table for survival AgD

## Usage

``` r
.build_arm_summary(
  data,
  arms,
  arm_vec,
  study_vec,
  trt_vec,
  cov_means,
  cov_sds,
  cov_names
)
```
