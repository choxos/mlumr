# Absolute survival predictions standardized to a target population

Absolute survival predictions standardized to a target population

## Usage

``` r
.predict_target_survival(object, newdata, type, summary, probs, times = NULL)
```
