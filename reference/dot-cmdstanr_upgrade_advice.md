# The advice that goes with a positive `.cmdstanr_too_old_for_windows()`. Shared by `mlumr_engine()`, which reports it and leaves the engine alone, and by the fit-time engine resolution, which stops with it.

The advice that goes with a positive
[`.cmdstanr_too_old_for_windows()`](https://choxos.github.io/mlumr/reference/dot-cmdstanr_too_old_for_windows.md).
Shared by
[`mlumr_engine()`](https://choxos.github.io/mlumr/reference/mlumr_engine.md),
which reports it and leaves the engine alone, and by the fit-time engine
resolution, which stops with it.

## Usage

``` r
.cmdstanr_upgrade_advice()
```
