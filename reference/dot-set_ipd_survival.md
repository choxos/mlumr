# Set up survival IPD (internal; dispatched from [`set_ipd()`](https://choxos.github.io/mlumr/reference/set_ipd.md))

Set up survival IPD (internal; dispatched from
[`set_ipd()`](https://choxos.github.io/mlumr/reference/set_ipd.md))

## Usage

``` r
.set_ipd_survival(
  data,
  treatment,
  covariates,
  study,
  Surv,
  time,
  status,
  entry_time
)
```
