# Validate survival-specific prediction grids and spline controls

Validate survival-specific prediction grids and spline controls

## Usage

``` r
.validate_survival_controls(
  pred_times,
  rmst_horizon,
  mspline_degree,
  n_knots,
  n_rmst_grid = 100L,
  distribution = NULL,
  knots = NULL
)
```
