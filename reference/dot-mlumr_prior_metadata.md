# Store user priors plus the resolved Stan-scale beta prior

Store user priors plus the resolved Stan-scale beta prior

## Usage

``` r
.mlumr_prior_metadata(
  data,
  family,
  prior_intercept,
  prior_beta,
  prior_sigma,
  beta_fields,
  sd_x
)
```
