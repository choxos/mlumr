# Refuse anything but a survival `mlumr_data` for `geom_km()`

Refuse anything but a survival `mlumr_data` for
[`geom_km()`](https://choxos.github.io/mlumr/reference/geom_km.md)

## Usage

``` r
.validate_km_data(data)
```
